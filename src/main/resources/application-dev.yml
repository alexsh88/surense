spring:
  datasource:
    url: jdbc:mysql://localhost:3306/surense_dev?createDatabaseIfNotExist=true
    username: root
    password: root

logging:
  level:
    com.surense: DEBUG
    org.flywaydb: DEBUG
